# 화면 설계 토큰

`jeongsi.html`이 지키는 시각 규칙을 적어 둔다. 값을 바꿀 때는 여기부터 고친다.

## 색 — 액센트는 앰버 하나

| 토큰 | 값 | 쓰는 곳 |
|------|-----|---------|
| `--bg` | `#FAF9F7` | 페이지 배경(따뜻한 오프화이트) |
| `--surface` | `#FFFFFF` | 카드·표 표면 |
| `--line` | `#E8E6E2` | 1px 구분선·테두리 |
| `--line-strong` | `#D8D5CF` | 입력 테두리 |
| `--ink` | `#1A1A1A` | 본문 |
| `--ink2` | `#2B2B2B` | 보조 본문 |
| `--muted` | `#6B6B6B` | 설명·캡션 (오프화이트 위 5.1:1) |
| `--amber` | `#F59E0B` | 강조면·막대·아이콘 **글자색으로 쓰지 않는다** |
| `--amber-soft` | `#FEF3C7` | 선택된 칩·배지 배경 |
| `--amber-mid` | `#FCD9A0` | 버튼 호버 |
| `--amber-ink` | `#B45309` | 링크·강조 글자·포커스 링 |
| `--err` | `#9F2D20` | 오류·경고 글자 (의미색, 액센트 아님) |

차트 안에서만 쓰는 중립 채움 `#DDD9D2`(닿지 않는 구간)를 더한다.
앰버와의 CVD 분리 ΔE 18.2로 확인했다.

## 글자 크기 — 다섯 단계, 인접 단계 20% 이상

| 크기 | 쓰는 곳 |
|------|---------|
| 11px | 표 안 배지·각주·축 눈금 (기능성 텍스트 하한) |
| 13px | 표 셀·라벨·힌트·캡션·보조 설명 |
| 16px | 본문·입력·버튼·탭 (입력은 16px 미만으로 내리지 않는다 — iOS 확대) |
| 20px | 섹션 제목·요약 수치 |
| 25px | 페이지 제목·큰 수치 |

굵기는 400 / 600 / 700 세 단계. 행간 본문 1.65, 제목 1.3.
자간 -0.005em. 본문 줄 길이 `max-width: 42em`(한글 42자).

## 간격 — 4의 배수

4 / 8 / 12 / 16 / 20 / 24 / 28 / 32 / 48.
1~3px는 테두리와 광학 보정에만 쓴다.
같은 묶음 안은 좁게, 묶음 사이는 넓게. 제목 위 여백이 아래보다 크다.

## z-index 단계표

| 값 | 쓰는 곳 |
|----|---------|
| 20 | 표의 고정 열(군·대학·관심) |
| 30 | 표의 고정 머리행 |

## 하지 않는 것

그라데이션, 그림자(0.04 초과), 보라 계열, 이모지, 카드 안 카드,
제목 밑줄·좌측 장식 바, 다크 모드 선제 추가.

## 점검

```bash
npx impeccable detect jeongsi.html
```

Playwright로 320 / 390 / 768 / 1280 폭을 찍고, 가로 스크롤·요소 잘림·
한글 줄바꿈·인쇄 뷰를 확인한다.
